# Calamares fstab module - output is overwritten afterwards by
# abroot-install-fstab.sh with the canonical immutable layout, so keep the
# module in the sequence (other jobs depend on it) but with plain options.
---
ssdExtraOptions:
  -
options: [ noatime ]
